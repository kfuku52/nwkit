# 監査指摘への対応（0.43.25）

- `shift-simulate` の生成パラメータ・モデル・真値・潜在値を監査対象へ登録し、監査先との衝突を処理前に拒否する。
- `annotate` の入力TSVと出力、入力Newickと補助レポートの衝突を拒否する。主Newickの意図的な同一ファイル更新は維持する。
- スカラー探索は最良グリッド候補を必ず精密化し、採用解の近傍で確認された収束だけを成功とする。
- 単独Newick出力をトランザクション化する。外側のトランザクション内では登録済みステージの検証付き記述子を再利用し、inode保護を維持する。
- 名前変換TSVの重複・空ヘッダー、行幅不一致を拒否する。
- Mkテストは内部状態の全列挙と行列指数による独立尤度計算を追加し、境界診断を実際の推定レートに対応させた。既存の尤度精度要求は維持している。

同一パス・シンボリックリンク・ハードリンクによる衝突、途中書込失敗、外側トランザクション、5つの極小を持つ目的関数、別の極小からの収束誤認を回帰テストにした。

## 検証環境

共有作業ツリーで別タスクのテスト削減が進行していたため、ベースコミット `8dddd3e` のdetached worktreeへ今回の変更だけをコピーして検証した。別タスクの変更はこの修正に含めていない。

Python 3.14.7 / macOS ARM64。`constraints-dev.txt` に従う独立環境を作り、editable installを実施した。ETE4のキャッシュ済みバイナリが読み込めなかったため、同じ4.4.0をキャッシュなしでソースから再構築した。製品の依存関係や検証条件を緩めてはいない。

## 結果

- 修正後の再現結果: [fixed-reproduction.json](fixed-reproduction.json)。元ファイルが保持され、最適化の目的関数値は既知の最良値と一致する。
- 最初の関連テスト: 187成功。
- トランザクション関連: 114成功。全体検証中に判明した3つの複数出力ケースも個別再検証で成功。
- 最終 `python tools/check.py release`: 終了コード0。
- 全テスト: **4090 passed, 83 skipped**。失敗なし。所要407.71秒（性能比較ではなく実行記録）。
- branch coverageを含むcoverage report: **85%**、必要基準80%を達成。
- Ruff lint/format、mypy、pip check、Bandit、依存関係監査、複雑度のハード上限: すべて成功。
- wheel/sdistの独立ビルド、メタデータ・内容検査、バイト単位の再現性: 成功。
- 別OS・別Pythonおよびスキップされた外部依存ケースはこのローカル検証では実行していない。

要約ログ: [checks/release-summary.log](checks/release-summary.log)。
