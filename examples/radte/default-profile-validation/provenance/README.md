# Executed runner snapshot

`validate_radte_default_profile.py` contains the exact runner bytes recorded in
the pre-fix primary/stress metadata. The maintained tool in `tools/` is later
formatted and guarded against nonfinite returned estimates. These guards retain
failed observations and previously completed point estimates; they do not alter
the recorded 650-family results or the dating estimator. This archived copy is not a
second entry point: restore it to `tools/validate_radte_default_profile.py` in
a source checkout matching the metadata to replay the historical experiment.
Input hashes and per-family seeds are retained in the adjacent evidence files.
